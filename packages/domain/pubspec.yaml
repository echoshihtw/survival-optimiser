name: domain
description: Pure Dart domain layer - no Flutter dependencies
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: '^3.11.5'

dependencies:
  fpdart: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
